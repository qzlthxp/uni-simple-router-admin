// 本地后端
export const baseUrl = 'localhost:8080/api'

// 本地存储命名
export const storageConfig = {
  userPass: 'USER_PASS', // 保存当前用户登录名和密码（自动登录）
  userInfo: 'USER_INFO', // 当前用户信息
  userToken: 'USER_TOKEN', // token
  userList: 'USER_LIST' // 所有用户列表
}
